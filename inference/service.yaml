apiVersion: v1
kind: Service
metadata:
  name: inference-service
  labels:
    app: proj-inference
spec:
  selector:
    app: proj-inference
  type: NodePort
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
